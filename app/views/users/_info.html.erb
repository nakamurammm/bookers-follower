<table class='table'>
  <tr><%= attachment_image_tag(user, :profile_image, :fill, 100, 100, fallback: "no_image.jpg") %></tr>
  <tr>
	  <th>name</th>
	  <th><%= user.name %></th>
  </tr>
  <tr>
	  <th>introduction</th>
	  <th><%= user.introduction %></th>
  </tr>
  <tr>
    <th>follows</th>
    <th><%= user.follower.count %></th>
  </tr>
  <tr>
    <th>followers</th>
    <th><%= user.followed.count %></th>
  </tr>
</table>

<% if current_user != user %>
 　<% if current_user.following?(user) %>
   　<%= link_to 'フォロー外す', relationship_path(user.id), method: :delete, class: "btn btn-default" %>
   <% else %>
   　<%= link_to 'フォローする', relationships_path(user.id), method: :POST , class: "btn btn-primary"%>
   <% end %>
 <% end %>

<div class='row'>
  <% if user.id == current_user.id %>
    <%= link_to edit_user_path(current_user.id) do %>
      <div class="btn btn-outline-secondary btn-block fas fa-user-cog"></div>
    <% end %>
  <% else %>
    <%= link_to edit_user_path(user.id) do %>
      <div class="btn btn-outline-secondary btn-block fas fa-user-cog"></div>
    <% end %>
  <% end %>
</div>